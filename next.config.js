// /** @type {import('next').NextConfig} */
// const nextConfig = {}

// module.exports = nextConfig
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    unoptimized: true,
        // domains: ['theme905-computer-shop.myshopify.com', 'example.com', 'ecomapi.ftdigitalsolutions.org', 'www.primeabgb.com', "semantic-ui.com"],
    },
};

module.exports = nextConfig;
